<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar_ProfilePage">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="profile_page_banner_title">
        <item name="android:textSize">@dimen/profile_page_banner_title</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="profile_page_titles">
        <item name="android:textSize">@dimen/profile_page_titles</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:gravity">bottom</item>
        <item name="android:paddingTop">18dp</item>
        <item name="android:paddingStart">@dimen/text_margin</item>
        <item name="android:paddingEnd">@dimen/text_margin</item>
        <item name="android:paddingBottom">@dimen/profile_page_titles_vertical_padding</item>
    </style>

</resources>
